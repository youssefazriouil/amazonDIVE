Hi {{ user.name }},

Please open the following link to setup a new password for your DIVE user account.

{{ url('dive_front_user_newpassword', { id: user.id, hash: user.hash } ) }}

If you didn't request a new password, you can ignore this email.