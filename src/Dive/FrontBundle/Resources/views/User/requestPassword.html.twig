{% if error %}
    <div class="error">{{ error.message }}</div>
{% endif %}
<form name="form" id="user-request-password" method="post" action="{{ path('dive_front_user_requestpassword') }}">
{{ form_rest(form) }}
</form>